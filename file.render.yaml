services:
  - type: web
    name: data-manager-app
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: DATABASE_URL
        value: data.db
